import { ExperiencesPipe } from './experiences.pipe';

describe('ExperiencesPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperiencesPipe();
    expect(pipe).toBeTruthy();
  });
});
