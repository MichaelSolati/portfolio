import { ImgLoaderDirective } from './img-loader.directive';

describe('ImgLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
